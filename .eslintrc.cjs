module.exports = {
  // Use Next's recommended base config for compatibility with `next lint`
  extends: ["next/core-web-vitals"],
  rules: {
    // Keep project-specific overrides minimal; tweak later if needed
    "no-console": "warn"
  }
}
